var modem = require('../lib/gsm_modem.js');

var tsel_hlr = [];
var isat_hlr = [];

function filter_number(number)  {
    // if(number match tsel_hlr ) {
    //      emit data based on hlr
    // }
    //
    // if(number start with 0) {
    //      convert to 62 based prefix
    //
    // }
}
